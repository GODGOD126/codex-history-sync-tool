{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Codex History Sync desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
